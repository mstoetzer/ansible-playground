---

# This Playbook prints Hello World

- hosts: all
  gather_facts: False
  become: no
  become_method: sudo
  remote_user: "{{ admin_username }}"

  tasks:
    - name: List Home Dir
      raw: ls -al ~
      args:
        executable: /bin/bash
